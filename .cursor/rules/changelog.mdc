---
description: 开发过程中的变更记录规范
globs: "**/*"
alwaysApply: true
---

# 开发变更记录

本项目的需求稿维护在 PM Agent 工作区：
- 项目上下文：`GenerateUI/projects/auto-so-me/project-context.md`
- v0.1 需求稿：`GenerateUI/projects/auto-so-me/sprints/v0.1/requirement.md`
- v0.2 需求稿：`GenerateUI/projects/auto-so-me/sprints/v0.2/requirement.md`

Vibe-coding 过程中实现可能偏离需求稿，按以下规则记录：

## 何时记录

当你完成的改动属于以下任一类型时，**必须**追加到 `CHANGELOG.md`：

1. **砍功能**：需求稿中有但实现时决定不做或降级
2. **加功能**：需求稿中没有但实现时新增的能力
3. **改流程**：交互顺序、状态机、文件结构与需求稿不同
4. **改接口**：CLI 参数、frontmatter 字段、目录命名与需求稿约定不同
5. **改策略**：角度体系、脱敏规则、调性规则等核心策略的调整

## 不需要记录

- Prompt 用词微调（换个说法、加个 emoji 规则）
- 代码重构（不影响外部行为）
- Bug 修复（修的是实现问题，不是需求变更）

## 记录格式

在项目根目录的 `CHANGELOG.md` 中追加，格式：

## 示例

2026-03-23
变更
涉及功能：F4 封面图生成
需求稿原设计：每篇文案必须生成封面图
实际实现：封面图改为可选，用户可跳过
原因：GenerateImage 中文渲染质量不稳定，强制生成反而拖慢流程
新增
涉及功能：F3 文案生成
需求稿原设计：无此功能
实际实现：新增 --tone 参数，支持用户指定文案语气风格
原因：测试中发现不同产品需要不同调性，固定调性不够灵活


## 注意

- 每次变更写 2-3 行就够，不要写成需求文档
- 这不是 git commit log，只记**与需求稿的偏差**
- 方向性大变（如砍掉整个平台、改变目标用户）除了记在这里，还需提醒用户回 PM Agent 工作区更新 `project-context.md`
